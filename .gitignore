# Secrets / env
.env
.env.*
!.env.example

# Private project context for Claude Code (infra details, roadmap,
# unreleased plans) - never meant for the public repo
CLAUDE.md

# Same reasoning as CLAUDE.md above - hardcodes pinas01's IP/hostnames/user
# choices, not app logic. Lives in the encrypted CLAUDE.md/memory backup
# pipeline instead (see CLAUDE.md "Component sub-tree" / backup section).
/tools/bootstrap-dev-vm.sh
/tools/backup-to-pinas01.sh

# Machine-local Claude Code permission allowlist (settings.json next to it
# is shared on purpose; this one is per-clone scratch)
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
.venv/
venv/

# Logs
*.log
logs/

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/

# Docker
*.pid

# Local checklist-history SQLite DB (see README "История чек-листов")
/app/data/
*.db
*.db-wal
*.db-shm

# Backups / temp
*.bak
*.tmp
